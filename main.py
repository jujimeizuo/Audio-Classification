import argparse

# from BirdDataset import *
# from model import *
# from train import *
# from test import *
# from test import *


def main(dataset_path):
    # # Load the dataset
    # dataset = BirdAudioDataset(dataset_path)
    # train_dataloader = DataLoader(dataset, batch_size=4, shuffle=True, num_workers=2)
    # test_dataloader = DataLoader(dataset, batch_size=4, shuffle=True, num_workers=2)
    # predict_dataloader = DataLoader(dataset, batch_size=4, shuffle=True, num_workers=2)

    # # Create the model
    # model = SimpleCNN()

    # # Loss function and optimizer
    # criterion = nn.CrossEntropyLoss()
    # optimizer = optim.SGD(model.parameters(), lr=0.01)

    # # Train the model
    # train(model, train_dataloader, criterion, optimizer, epochs=2)

    # # Test the model
    # test(model, test_dataloader)

    # # Predict
    # result = predict(model, predict_dataloader)

    # # Save the model
    pass


if __name__ == '__main__':
    # parser = argparse.ArgumentParser(description='Bird Audio classification')
    # parser.add_argument('dataset_path', default="./dataset", help='Path to the dataset')


    # options = parser.parse_args()
    # main(
    #     options.dataset_path
    # )
    pass